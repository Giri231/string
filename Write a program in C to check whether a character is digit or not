#include<iostream>

using namespace std;

int main()
{
    string s;
    getline(cin,s);
    int n=s.length();
    int flag;
    for(int i=0;i<n;i++)
    {
        if(s[i]>='0' && s[i]<='9')
        {
       flag=0;
        }
    }
 if(flag==0)
 {
           cout<<"digit";
 }
 else
 {
     cout<<"not digit";
 }
}
