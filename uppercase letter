#include<iostream>

using namespace std;

int main()
{
    string s;
    getline(cin,s);
   int n=s.length();
   for(int i=0;i<n;i++)
   {
       if( ! (s[i]>='A' && s[i]<='Z') )
       {
           cout<<"The entered letter is not an UPPERCASE letter";
       }
   }
    
}
